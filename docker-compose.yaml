version: "3.2"
services:
  hugo:
    image: klakegg/hugo:0.101.0
    command: >
      server 
        --environment ${HUGO_ENVIRONMENT}
        --appendPort=false
        --disableFastRender 
        --port ${HUGO_PORT}
        --baseURL ${HUGO_BASEURL}
    volumes:
      - "${PWD}:/src"
    ports:
      - "${HUGO_PORT}:${HUGO_PORT}"
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "5"
